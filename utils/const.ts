export const REGEX = {
  phone: /^((\+7|7|8)+([0-9]){10})$|\b\d{3}[-.]?\d{3}[-.]?\d{4}/,
  email: /[a-z0-9]+@[a-z]+\.[a-z]{2,3}/,
}

export const ROUTES = {
  HOME: '/',
  OBJECTS: '/objects/',
  CATALOG: '/catalog/',
  ABOUT: '/about/',
  CONTACTS: '/contacts/',
  ARTICLES: '/articles/',
}
